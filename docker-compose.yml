services:
  ctg:
    build: .
    ports:
      - '3001:3001'
    env_file:
      - .env
    volumes:
      - ./keys:/app/keys
      - ./files:/app/files
      - ./download:/app/download
      - ./log:/app/log
      - ./template.config.json:/app/config.json
    restart: unless-stopped
