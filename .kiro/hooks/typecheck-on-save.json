{
  "version": "v1",
  "hooks": [
    {
      "name": "TypeScript Check on Save",
      "trigger": "PostFileSave",
      "description": "Run TypeScript type checking after saving a .ts or .tsx file to catch errors early",
      "matcher": "\\.(ts|tsx)$",
      "action": {
        "type": "command",
        "command": "cd web && npx tsc --noEmit 2>&1 | head -20",
        "timeout": 30
      }
    }
  ]
}